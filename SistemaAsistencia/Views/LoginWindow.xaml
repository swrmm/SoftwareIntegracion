<Window x:Class="SistemaAsistencia.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SistemaAsistencia.Views"
        mc:Ignorable="d"
        Title="Iniciar Sesión" Height="520" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    

    <Border Background="{StaticResource BgDark}" CornerRadius="16" BorderBrush="{StaticResource BorderDark}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Button Style="{StaticResource IconButton}" Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Width="30" Height="30" Click="CloseButton_Click" Foreground="{StaticResource TextSecondary}"/>
            </Grid>
            
            <!-- Main Content -->
            <StackPanel Grid.Row="1" Margin="40,20,40,40">
                <!-- Logo/Icon -->
                <TextBlock Text="🏢" FontSize="60" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                
                <!-- Headers -->
                <TextBlock Text="Control de Asistencia" Style="{StaticResource HeaderText}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="Ingrese sus credenciales" Style="{StaticResource NormalText}" HorizontalAlignment="Center" Margin="0,0,0,32"/>
                
                <!-- Email -->
                <TextBlock Text="Correo Electrónico" Style="{StaticResource SmallText}" Margin="0,0,0,4"/>
                <TextBox x:Name="txtCorreo" Style="{StaticResource DarkTextBox}" Text="{Binding Correo, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" KeyDown="CorreoTextBox_KeyDown"/>
                
                <!-- Password -->
                <TextBlock Text="Contraseña" Style="{StaticResource SmallText}" Margin="0,0,0,4"/>
                <PasswordBox x:Name="txtPassword" Style="{StaticResource DarkPasswordBox}" Margin="0,0,0,24" KeyDown="PasswordBox_KeyDown"/>
                
                <!-- Error Message -->
                <TextBlock Text="{Binding MensajeError}" Foreground="{StaticResource Error}" Margin="0,0,0,16" TextWrapping="Wrap" HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource NormalText}">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding MensajeError}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding MensajeError}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <!-- Login Button -->
                <Button Content="Iniciar Sesión" Style="{StaticResource PrimaryButton}" 
                        Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}" 
                        Height="45" FontSize="16"/>
                
                <!-- Footer -->
                <TextBlock Text="© 2024 Sistema de Asistencia" Style="{StaticResource SmallText}" HorizontalAlignment="Center" Margin="0,32,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
