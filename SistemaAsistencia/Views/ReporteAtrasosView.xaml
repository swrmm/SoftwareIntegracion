<UserControl x:Class="SistemaAsistencia.Views.ReporteAtrasosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Atrasos" Style="{StaticResource HeaderText}"/>
            <TextBlock Text="Entradas después de las 09:30" Style="{StaticResource NormalText}" Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- Filters -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,20" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondary}"/>
                    <DatePicker Style="{StaticResource DarkDatePicker}" SelectedDate="{Binding FechaInicio}" Width="120"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondary}"/>
                    <DatePicker Style="{StaticResource DarkDatePicker}" SelectedDate="{Binding FechaFin}" Width="120"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <TextBlock Text="{Binding TotalRegistros, StringFormat='Total: {0}'}" VerticalAlignment="Center" Margin="0,0,20,0" Style="{StaticResource SubHeaderText}" Foreground="{StaticResource AccentPrimary}"/>
                    <Button Content="Generar Reporte" Command="{Binding GenerarReporteCommand}" Style="{StaticResource PrimaryButton}" Padding="22,12" MinHeight="44"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Data Grid -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <Grid>
                <DataGrid ItemsSource="{Binding Registros}" Style="{StaticResource DarkDataGrid}" Margin="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Empleado" Binding="{Binding NombreEmpleado}" Width="1.5*"/>
                        <DataGridTextColumn Header="Correo" Binding="{Binding Correo}" Width="2*"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="130"/>
                        <DataGridTextColumn Header="Hora de Entrada" Binding="{Binding HoraEntradaTexto}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
                
                <TextBlock Text="No se encontraron registros de atrasos" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource NormalText}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SinResultados}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
