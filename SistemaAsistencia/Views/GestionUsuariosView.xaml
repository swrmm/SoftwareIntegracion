<UserControl x:Class="SistemaAsistencia.Views.GestionUsuariosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header & Actions -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Gestión de Usuarios" Style="{StaticResource HeaderText}" Grid.Column="0"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Actualizar" Command="{Binding CargarUsuariosCommand}" Style="{StaticResource SecondaryButton}" Padding="22,12" MinWidth="130" Margin="0,0,12,0"/>
                <Button Content="Nuevo Usuario" Command="{Binding NuevoUsuarioCommand}" Style="{StaticResource PrimaryButton}" Padding="24,12" MinWidth="150"/>
            </StackPanel>
        </Grid>
        
        <!-- Status Message -->
        <Border Grid.Row="1" Background="{StaticResource Success}" CornerRadius="8" Padding="12" Margin="0,0,0,20" 
                Visibility="{Binding MensajeEstado, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding MensajeEstado}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MensajeEstado}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding MensajeEstado}" Foreground="{StaticResource BgDark}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- Width for form panel -->
                <ColumnDefinition Width="380">
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                            <Setter Property="Width" Value="380"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding MostrarFormulario}" Value="False">
                                    <Setter Property="Width" Value="0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ColumnDefinition.Style>
                </ColumnDefinition>
            </Grid.ColumnDefinitions>

            <!-- Data Grid -->
            <Border Grid.Column="0" Style="{StaticResource CardBorder}" Padding="0" Margin="0,0,20,0">
                <DataGrid ItemsSource="{Binding Usuarios}" Style="{StaticResource DarkDataGrid}" Margin="1"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="1.5*" MinWidth="160"/>
                        <DataGridTextColumn Header="Correo" Binding="{Binding Correo}" Width="2*" MinWidth="220"/>
                        <DataGridTextColumn Header="Rol" Binding="{Binding Rol}" Width="130" MinWidth="120"/>
                        
                        <DataGridTemplateColumn Header="Acciones" Width="200" MinWidth="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Button Content="Editar" Style="{StaticResource SecondaryButton}" Padding="12,5" Margin="0,0,6,0" FontSize="12" MinWidth="75"
                                                Command="{Binding DataContext.EditarUsuarioCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}"/>
                                        <Button Content="Eliminar" Style="{StaticResource DangerButton}" Padding="12,5" FontSize="12" MinWidth="80"
                                                Command="{Binding DataContext.EliminarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Form Panel -->
            <Border Grid.Column="1" Style="{StaticResource CardBorder}" Padding="20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Form Title -->
                        <TextBlock Margin="0,0,0,24">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource SubHeaderText}">
                                    <Setter Property="Text" Value="Nuevo Usuario"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ModoEdicion}" Value="True">
                                            <Setter Property="Text" Value="Editar Usuario"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Nombre -->
                        <TextBlock Text="Nombre" Style="{StaticResource SmallText}" Margin="0,0,0,6"/>
                        <TextBox Style="{StaticResource DarkTextBox}" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16"/>

                        <!-- Correo -->
                        <TextBlock Text="Correo Electrónico" Style="{StaticResource SmallText}" Margin="0,0,0,6"/>
                        <TextBox Style="{StaticResource DarkTextBox}" Text="{Binding Correo, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16"/>

                        <!-- Contraseña -->
                        <TextBlock Text="Contraseña" Style="{StaticResource SmallText}" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="txtFormPassword" Style="{StaticResource DarkPasswordBox}" Margin="0,0,0,4"/>
                        <TextBlock Foreground="{StaticResource Warning}" Margin="0,0,0,16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource SmallText}">
                                    <Setter Property="Text" Value="Dejar vacío para mantener la misma contraseña"/>
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ModoEdicion}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Rol -->
                        <TextBlock Text="Rol de Usuario" Style="{StaticResource SmallText}" Margin="0,0,0,6"/>
                        <ComboBox Style="{StaticResource DarkComboBox}" SelectedIndex="{Binding FormRolIndex}" Margin="0,0,0,24">
                            <ComboBoxItem Content="Empleado" Tag="Empleado"/>
                            <ComboBoxItem Content="Administrador" Tag="Administrador"/>
                        </ComboBox>

                        <!-- Form Actions -->
                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Button Grid.Column="0" Content="Cancelar" Command="{Binding CancelarCommand}" Style="{StaticResource SecondaryButton}" MinHeight="46" Padding="20,12" Margin="0,0,6,0"/>
                            <Button Grid.Column="1" Content="Guardar" Command="{Binding GuardarCommand}" CommandParameter="{Binding ElementName=txtFormPassword}" Style="{StaticResource PrimaryButton}" MinHeight="46" Padding="20,12" Margin="6,0,0,0"/>
                        </Grid>
                        
                        <!-- Form Error -->
                        <TextBlock Text="{Binding FormError}" Foreground="{StaticResource Error}" Margin="0,16,0,0" TextWrapping="Wrap" HorizontalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource NormalText}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding FormError}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding FormError}" Value="">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
